import 'package:flutter/material.dart';

/// Dark mode theme configuration.
ThemeData darkMode = ThemeData(
    // Custom color scheme for dark mode.
    colorScheme: const ColorScheme.light(
        background: Colors.black,      // Background color in dark mode.
        primary: Colors.white,         // Primary text and icon color in dark mode.
        secondary: Colors.orange,      // Secondary color in dark mode.
    ),
);
